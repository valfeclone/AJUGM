$(document).ready(() => {
    // Check team name, university, member name, faculty, major does not contain symbols (.no-symbols)
    

    // Check password length (8-30 characters) (#password)

    // Check if re-password is identical to password (#repassword)

    // Check LinkedIn format (.linkedin)

    // Check KTM file format and make sure not empty (.ktm)

    // Check if select does not select 'default' value (.select)

});